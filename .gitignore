# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
!*.tfvars.example
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# .terraform.lock.hcl is intentionally committed (reproducible provider versions)

# Secrets / keys
*.pem
*.key
.env

# OS
.DS_Store
Thumbs.db

# Credential files (AWS access key CSV, etc.) — never commit
*.csv
